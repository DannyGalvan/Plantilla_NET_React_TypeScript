# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
# pnpm is the canonical package manager for project.client. A stray
# `npm install` regenerates package-lock.json - ignore it so it
# cannot be accidentally reintroduced.
package-lock.json
# F11: never commit .env files. Only .env.example is allowed.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# heroui-agents-md
.heroui-docs/
# Graphify cache (project-local, not versioned)
graphify-out/
